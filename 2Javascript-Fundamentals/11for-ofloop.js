let greetings = "Hello welcome from geeks for geeks";
//we can iterate over strings using for of loop

for(let char of greetings){
    console.log(char);
    console.log(char.charCodeAt(0)); //prints the unicode value of the character
}